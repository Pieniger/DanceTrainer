<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- App theme inheriting Material3 (XML) so AAPT can resolve it -->
    <style name="Theme.DanceTrainer" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Optional: tweak system bars -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>

</resources>
